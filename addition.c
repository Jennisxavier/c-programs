/*addition of two integers*/
#include <stdio.h>
int main()
{
    int f,s,sum;
    printf("enter the first no: ");
    scanf("%d",&f);
    printf("enter the second no:");
    scanf("%d",&s);
    sum=f + s;
    printf("the sum is:%d",sum);
    return 0;
}