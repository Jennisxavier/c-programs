#include <stdio.h>
int read(int arr,int size, int size);
int multiplication(int arr1, int arr2; int size; int size);
int main()
{
    int matA[2][2]={{1,2},{3,4}}, matB[2][2]={{1,0},{0,1}};
    int product;
    product=multiplication(A,B,2,2);
    printf("The product of the two matrix is: %d",product);
    return 0;
}
